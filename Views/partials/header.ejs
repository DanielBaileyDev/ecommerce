<header>
    <div class="header-container">
        <div class="logo">
            <i class="fa-solid fa-tags logo-icon"></i>
            <h3>Ecommerce</h3>
        </div>
        <nav>
            <ul>
                <a href="/">
                    <li class="header-link <% if(url === ''){ %>active<% } %>">Home</li>
                </a><!--
                --><a href="/products">
                    <li class="header-link <% if(url === '/products'){ %>active<% } %>">Products</li>
                </a><!--
                --><a href="#">
                    <li class="header-link <% if(url === '/about'){ %>active<% } %>">About</li>
                </a>
            </ul>
        </nav>
        <div class="login">
            <% if(!loggedIn){ %>
                <span class="header-link" onclick="loginOverlay()">Login</span>
                <span class="header-link" onclick="signUpOverlay()">Sign Up</span>
                <% } else { %>
                    <div class="dropdown">
                        <span class="header-link dropbtn" onclick="dropDown()">
                            <%= username %> <i class="fa-solid fa-caret-down"></i>
                        </span>
                        <div id="myDropdown" class="dropdown-content">
                            <a href="/admin" class="header-link <% if(url === '/admin'){ %>active<% } %>">Admin</a>
                            <form class="logout-form" action="/auth/logout" method="POST">
                                <input class="header-link" type="submit" value="Logout">
                            </form>
                        </div>
                    </div>
                    <% } %>
        </div>
    </div>
</header>
<%- include('overlay') -%>